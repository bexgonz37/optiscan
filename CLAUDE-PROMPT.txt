=== CRITICAL: CORRECT FOLDER ===
Repo: https://github.com/bexgonz37/optiscan (private)
ONLY use: C:\Users\bexgo\Downloads\optiscan-main
DO NOT use: C:\Users\bexgo\optiscan (OLD - stale, wrong)

Verify before any work:
  cd C:\Users\bexgo\Downloads\optiscan-main
  git log -1 --oneline
Must be commit 58dae80 or newer.

These files must exist (else wrong folder):
  lib/early-accuracy.ts, docker-compose.yml, scripts/vps-setup.sh

In Cursor: Open Folder -> C:\Users\bexgo\Downloads\optiscan-main

=== TASK ===
You are working on OptiScan, Next.js 15 trading scanner.
User has top-tier Massive/Polygon real-time plan.
Deploy VPS, audit/improve app, add regular-stocks mode with session routing.

=== PART 1: VPS DEPLOY ===
Docker on Ubuntu 24.04 VPS (~$6/mo DigitalOcean or Hetzner).
Repo has: Dockerfile, docker-compose.yml, env.production.example, scripts/vps-setup.sh
Boot starts scanner+tracker via instrumentation.ts. Port 8780. SQLite in data/

Steps:
1. Create VPS, SSH in
2. curl -fsSL https://raw.githubusercontent.com/bexgonz37/optiscan/main/scripts/vps-setup.sh | bash
3. Edit /opt/optiscan/.env.production: POLYGON_API_KEY, SCAN_API_TOKEN, DISCORD_WEBHOOK_URL
4. cd /opt/optiscan && docker compose up -d --build
5. curl http://localhost:8780/api/health
6. Secure: SSH tunnel ssh -L 8780:localhost:8780 root@IP
7. Write docs/VPS.md
8. Run scripts/audit-accuracy.mjs

=== PART 2: AUDIT OPTIONS SCANNER ===
Files: lib/scanner-loop.ts, lib/zero-dte.js, lib/alert-capture.ts, lib/trade-verdict.ts, lib/early-accuracy.ts, lib/alert-store.ts, app/alert-lab/page.tsx, components/AppNav.tsx
Goal: 70% early hit rate @ 5m on TRADE-at-capture. npm test, tsc, build.

=== PART 3: REGULAR STOCKS MODE ===
Premarket 4-9:30 AM ET = stock callouts only
Regular 9:30 AM-4 PM ET = 0DTE options only
After hours 4-8 PM ET = stock callouts only
Closed = no new callouts

Implement: marketSession() in lib/trading-session.ts, stock scanner, DB asset_class field, nav Options|Stocks|Alerts|Settings, Discord routing, tests.

=== CONSTRAINTS ===
Signals only. Single VPS. No AI in signals. Push to main on github.com/bexgonz37/optiscan

=== DELIVER ===
VPS instructions, audit summary, nav map, session table, test guide, commit SHAs.
