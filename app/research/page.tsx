"use client";

/**
 * /research — trader Research hub (Quant, Scanner, Strategy Lab, AI).
 * Operational research-platform overview moved to /pipeline-health/research-platform.
 */
import { ResearchHub } from "@/components/ResearchHub";

export default function ResearchPage() {
  return <ResearchHub />;
}
