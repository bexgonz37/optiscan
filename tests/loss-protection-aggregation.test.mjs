import test from "node:test";import assert from "node:assert/strict";import {aggregateLossProtection} from "../lib/research/options/loss-protection-aggregation.ts";
const T=Date.parse("2026-07-30T14:00:00Z"),m=(n,b)=>({markAtMs:T+n*60000,bid:b,ask:b+.04,quoteAgeMs:1,createdAtMs:T+n*60000});
test("aggregation reuses replay outputs, separates call/put, and never calls negative policy profitable",()=>{const exit={tradeId:1,alertId:"a",symbol:"X",side:"call",optionSymbol:"O:X",dte:1,strategyFamily:"x",marketRegime:null,timeBucket:null,entryQuality:null,entryFill:1,enteredAtMs:T,targetT1:1.2,targetT2:null,stop:.8,status:"EXITED",exitFill:.5,exitAtMs:T+120000,canonicalReturnPct:-50,exitReason:"x",marks:[m(1,.9),m(2,.5)]};const loss={tradeId:2,side:"put",dte:0,strategyFamily:"x",timeOfDay:"morning",entryAsk:1,enteredAtMs:T,canonicalReturnPct:-50,marks:[{atMs:T+60000,bid:.85,ask:.89,quoteAgeMs:1,weakening:true},{atMs:T+120000,bid:.85,ask:.89,quoteAgeMs:1,weakening:true}]};const r=aggregateLossProtection({exitTrades:[exit],lossTrades:[loss],minimumSupportedSample:1});const p=r.policies.find(x=>x.policy==="Loss: Hard loss -10%");assert.equal(p.putCount,1);assert.notEqual(p.verdict,"PROFITABLE_SUPPORTED");assert.equal(r.productionBehaviorChanged,false);});
