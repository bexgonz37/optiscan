import test from "node:test";import assert from "node:assert/strict";import Database from "better-sqlite3";import {recordOptionsResearchObservation} from "../lib/research/options/prospective-evidence.ts";
function db(){const d=new Database(":memory:");d.exec("CREATE TABLE options_research_observations (id INTEGER PRIMARY KEY,observation_key TEXT NOT NULL UNIQUE,observed_at_ms INTEGER,session_date TEXT,symbol TEXT,direction TEXT,thesis_fingerprint TEXT,opportunity_case_id TEXT,alert_id TEXT,strategy_family TEXT,candidate_state TEXT,readiness_state TEXT,authority_state TEXT,blockers_json TEXT,underlying_price REAL,vwap REAL,vwap_relationship TEXT,structure_state TEXT,momentum_state TEXT,option_symbol TEXT,option_bid REAL,option_ask REAL,quote_timestamp_ms INTEGER,quote_age_ms INTEGER,source TEXT,freshness_state TEXT,created_at_ms INTEGER)");return d}
test("prospective evidence is idempotent, millisecond keyed, and preserves null evidence",()=>{const d=db(),x={observedAtMs:Date.parse("2026-07-30T14:00:00Z"),symbol:"nvda",source:"test",thesisFingerprint:"t",candidateState:"READY"};assert.equal(recordOptionsResearchObservation(d,x),true);assert.equal(recordOptionsResearchObservation(d,x),true);assert.equal(d.prepare("SELECT * FROM options_research_observations").all().length,1);const r=d.prepare("SELECT * FROM options_research_observations").get();assert.equal(r.vwap,null);assert.equal(r.observed_at_ms,x.observedAtMs);assert.ok(r.observation_key)});
test("distinct lifecycle states retain distinct observations",()=>{const d=db(),x={observedAtMs:Date.parse("2026-07-30T14:00:00Z"),symbol:"NVDA",source:"test",thesisFingerprint:"t"};recordOptionsResearchObservation(d,{...x,candidateState:"CANDIDATE_DETECTED"});recordOptionsResearchObservation(d,{...x,candidateState:"READY",optionSymbol:"O:NVDA260731C00195000"});assert.equal(d.prepare("SELECT COUNT(*) n FROM options_research_observations").get().n,2)});
